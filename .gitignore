# Go build output
/vsx

# Local Claude Code settings (personal, not shared)
.claude/settings.local.json

# Claude Code worktrees (isolated workspaces, not shared)
.claude/worktrees/

# Personal agent-skill tooling (symlinks + content); not part of the product
.claude/skills/
.agents/
